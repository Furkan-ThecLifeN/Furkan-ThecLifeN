pwd şuanki baktığı konumu 
ls ise dosyaları gözüktürür
cd .. konumu geri alır
cd "konum ismi" o konuma yollar
mkdir "Klasör ismi " oluşturur
touch "dosya ismi" oluşturur
git init = anahtar görevi görür ve server açar arka planda kayıt için
git config --global user.name "isim"
git config --global user.email "email" 
git status Konumunun ayrıntısını verir ve kayıt edilmeyenleri söyler
git add . kayıt eder
git commit -m "first save"
git log save kayıtlı dosyalara geri dönmek için anahtar verir
git checkout "anahtar" -- .
git dif = değişiklikleri kontrol eder
git checkout "dosya ismi" yaparsak yapılan değişiklikleri geri alır
git rm "dosya/klasör ismi" onu siler
git mv "dosya ismi" "koycağın isim" isim değiştirir